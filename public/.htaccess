Options -MultiViews

RewriteEngine On

# Si la ressource existe, on la sert normalement
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Sinon on redirige tout vers index.php
RewriteRule ^ index.php [QSA,L]